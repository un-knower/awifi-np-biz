/**
* 版权所有： 爱WiFi无线运营中心
* 创建日期:2017年6月27日 下午4:27:56
* 创建作者：王冬冬
* 文件名称：PubUserTest.java
* 版本：  v1.0
* 功能：
* 修改记录：
*/
package com.awifi.np.biz.api.client.dbcenter.user.model;

import org.junit.Test;

public class PubUserTest {

    @Test
    public void test(){
        PubUser usr=new PubUser();
        usr.setEmail(null);
        usr.setAbhor(null);
        usr.setAddress(null);
        usr.setAuthId(null);
        usr.setBadges(null);
        usr.setBirthday(null);
        usr.setBirthdayStr(null);
        usr.setCollege(null);
        usr.setCompany(null);
        usr.setCreateDate(null);
        usr.setFaceInfo(null);
        usr.setHobby(null);
        usr.setId(null);
        usr.setIndustry(null);
        usr.setIntegralUsable(null);
        usr.setIntegralUsed(null);
        usr.setLeisurePlace(null);
        usr.setLevel(null);
        usr.setMajor(null);
        usr.setNativeStr(null);
        usr.setPhone(null);
        usr.setPolitical(null);
        usr.setPost(null);
        usr.setRemarks(null);
        usr.setSex(null);
        usr.setTelphone(null);
        usr.setTotalExp(null);
        usr.setUserCard(null);
        usr.setUserNick(null);
        usr.setUserRealname(null);
        usr.setWechat(null);
      
        usr.getEmail();
        usr.getAbhor();
        usr.getAddress();
        usr.getAuthId();
        usr.getBadges();
        usr.getBirthday();
        usr.getBirthdayStr();
        usr.getCollege();
        usr.getCompany();
        usr.getCreateDate();
        usr.getFaceInfo();
        usr.getHobby();
        usr.getId();
        usr.getIndustry();
        usr.getIntegralUsable();
        usr.getIntegralUsed();
        usr.getLeisurePlace();
        usr.getLevel();
        usr.getMajor();
        usr.getNativeStr();
        usr.getPhone();
        usr.getPolitical();
        usr.getPost();
        usr.getRemarks();
        usr.getSex();
        usr.getTelphone();
        usr.getTotalExp();
        usr.getUserCard();
        usr.getUserNick();
        usr.getUserRealname();
        usr.getWechat();
    }
    
}
